version: '3.9'
services:
  backend:
    container_name: backend
    restart: always
    build: ../backend  
    env_file: ../backend/deployment/.env
  frontend:
    container_name: frontend
    restart: always
    build: ../frontend
    ports:
      - 80:80
      - 443:443
    depends_on:
      - backend
  